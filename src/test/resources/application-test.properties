spring.application.name=entur-mcp-server-test

# MCP Server Configuration
spring.ai.mcp.server.protocol=STATELESS
server.port=0

# Test URLs (will be overridden in tests)
org.entur.otp.url=http://localhost:8080/graphql
org.entur.geocoder.url=http://localhost:8080/geocoder

# Logging configuration for tests
logging.level.org.springframework=WARN
logging.level.org.entur.mcp=DEBUG
logging.level.io.modelcontextprotocol=WARN
